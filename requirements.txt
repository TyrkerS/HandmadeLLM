# Core (install torch with the cu128 index for Blackwell/sm_120):
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.7
numpy
pyyaml
tqdm

# Data pipeline
datasets

# Dev
pytest

# Serving (Phase 4)
fastapi
uvicorn

# Compile / Triton kernels (Phase 7). On Windows use: pip install triton-windows
# triton

# Optional
# wandb          # experiment tracking (train.py --wandb)
# bitsandbytes   # Phase 2 8-bit optimizer (recent build for Blackwell)
