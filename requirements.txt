# Core — pinned for reproducibility.
# torch is a Blackwell/sm_120 build; install it from the cu128 index FIRST:
#   pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cu128
# then: pip install -r requirements.txt
torch==2.11.0
numpy==2.4.6
PyYAML==6.0.3
tqdm==4.69.0

# Data pipeline
datasets==5.0.0
pyarrow==25.0.0

# Dev / tests
pytest==9.1.1

# ---- optional extras (install as needed) ----
# Serving (Phase 4):        pip install -r requirements-serve.txt
# Compile / Triton kernels: pip install triton-windows   (Windows) or triton (Linux)
# Experiment tracking:      pip install wandb   (train/sft with --wandb)
